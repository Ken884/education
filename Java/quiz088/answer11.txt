るクラスを継承したクラスを作りました。ただし、スーパークラスではthrowsキーワードで、 明示的に例外が発生する可能性を示していません。
サブクラスでは、そのメソッドをオーバーライドする際、 throwsキーワードをつけました。問題ないでしょうか。

//非チェック例外であれば問題ないが、チェック例外をスローするとコンパイルエラー。
　スーパークラスのメソッドがスローを行っている場合、サブクラスでオーバーライドしてスローする例外は
　同じ例外か、その例外クラスのサブクラスの例外しかスローできない。
　ただし、非チェック例外はスーパークラスのメソッドに関係なくスローできる。。